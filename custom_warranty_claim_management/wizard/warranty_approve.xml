<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>

        <record id="warranty_approve_note_form" model="ir.ui.view">
            <field name="name">warranty.approve.note.form</field>
            <field name="model">approve.warranty.wizard</field>
            <field name="arch" type="xml">
                <form string="Approve Note">
                    <sheet>
                        <group>
                            <field name="note" required="1"/>
                            <field name="approval_decision" invisible="not is_last_approval_stage"/>
                            <field name="total_price" readonly="1" invisible="approval_decision not in ['refund']"/>
                            <field name="refund_amount" invisible="approval_decision not in ['refund']"/>
                        </group>
                    </sheet>
                    <footer>
                        <button name="approve_warranty"
                                string="Approve"
                                type="object"
                                class="btn-primary"
                                />
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_warranty_approve_note" model="ir.actions.act_window">
            <field name="name">Approve Note</field>
            <field name="res_model">approve.warranty.wizard</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="warranty_approve_note_form"/>
            <field name="target">new</field>
        </record>
    </data>
</odoo>